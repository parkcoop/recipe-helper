<form method="post" action="/edit/{{_id}}">
  <input type="text" name="id" value="{{recipeId}}" hidden>
  <input type="text" name="title" value="{{title}}"><br>

  <input name="time" type="number" value={{time}}><br>

  <textarea name="instructions" rows="10" cols="50">{{instructions}}"</textarea>
  <label>Ingredients</label>
  <div id="ingredients">
    {{#each ingredients}}
    <input name="ingredients" value="{{this}}">
    {{/each}}
  </div>
  <button type="button" id="add">Add more ingredients</a>
    <button type="submit">Submit</button>
</form>